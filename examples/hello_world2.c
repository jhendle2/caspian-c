#include <stdio.h>

int main(int argc, char** argv) {
    printf("Hello User #(%d)!", (5+(2*3)));
    int x = 1 + 2 * ( 3 + 4 * (5));
    return 0;
}

/* FIXME: Block comments probably still don't work
   FIXME: Structs, enums, unions, typedef */